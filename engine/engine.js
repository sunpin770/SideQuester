const scenes = {
    
}
const engineOptions = {
    canvasElementId: `screen`,
    height: 800,
    width: 1100,
    scenes: scenes,
    fixedUpdateFPS: 60,
};
const SQ = new ex.Engine(engineOptions);
SQ.onInitialze = () => {
    
}

